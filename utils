
ps -ef | grep "server.js"  | awk '{print "sudo kill -9 " $2}' | bash

ifconfig | grep -Eo 'inet (addr:)?([0-9]*\.){3}[0-9]*' | grep -Eo '([0-9]*\.){3}[0-9]*' | grep -v '127.0.0.1'

docker rm $(docker ps -a -q)

#jenkins
webhook --> ip:8080/github-webhook/ application/json
branch specifier: origin/feat/* or */feat/*
install ssh agent plugin on jenkins
add pem credential https://support.cloudbees.com/hc/en-us/articles/360029470091-How-to-use-the-SSH-Agent-Plugin?page=24

# ansible playbooks
copy pem to ansible
chmod 400 <>.pem
ssh-keygen
ansible-playbook add-key.yml -i ec2.py -l tag_Name_Front_EC2_Terra_juan_bolanosr,tag_Name_Back_EC2_Terra_juan_bolanosr --key-file ~/key-pair-JB.pem --user ubuntu
ansible-playbook ui.yml -i ec2.py -l tag_Name_Front_EC2_Terra_juan_bolanosr --user ubuntu
ansible-playbook api.yml -i ec2.py -l tag_Name_Back_EC2_Terra_juan_bolanosr --user ubuntu

# ansible dynamic inventory repo: ec2.py, ec2.ini
https://github.com/ansible/ansible/tree/stable-2.9/contrib/inventory

terraform apply -target=aws_security_group.rampup_sec_group_jenkins -target=aws_security_group.rampup_sec_group_ansible -target=aws_security_group.rampup_sec_group_hosts